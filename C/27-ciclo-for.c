#include <stdio.h>
#include <stdbool.h>
int main() {
	// Ciclo for
	for (int contador = 1; contador <= 5; contador++){
		printf("\nBuenos dias...\n");
		printf("Variable contador: %d\n", contador);
	}
	
	return 0;
}

