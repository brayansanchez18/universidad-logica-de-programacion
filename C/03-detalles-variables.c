#include <stdio.h>

int main()
{
  // declaramos una variable
  int miNumero;
  // la inicializamos
  miNumero = 10;
  // imprimimos la variable
  // formato %d -> decimal
  printf("mi numero es: %d", miNumero);
}